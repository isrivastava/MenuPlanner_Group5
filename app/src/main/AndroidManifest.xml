<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="library.group5">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".group5Activity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NewCustomerActivity"></activity>
        <activity android:name=".HomePageActivity"></activity>
        <activity android:name=".RecipeActivity"></activity>
        <activity android:name=".EventActivity"></activity>
        <activity android:name=".ManageListActivity"></activity>
        <activity android:name=".ShoppingListActivity"></activity>


    </application>

</manifest>